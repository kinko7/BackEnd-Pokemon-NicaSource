const { Pokemons, Types } = require("../db");
const { v4: uuidv4 } = require("uuid");

async function createPoke(req, res, next) {
  
}

async function updatePoke(req, res, next) {
   

} 

module.exports = {
  createPoke,
  updatePoke
};
